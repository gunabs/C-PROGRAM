
#include <stdio.h>
#include <limits.h>

int main() {
   
   int arr[]={5,2,7,4,9,1};
   int n=6;
  int  max=INT_MIN;
  
   for(int i=0;i<n;i++)
   {
       if(max<arr[i])
       {
           max=arr[i];
       }
       
   }
   
   
   printf("%d ",max);

    return 0;
}
