#include <stdio.h>

int main() {
   char s[50]="hello";
   char ch[]=" guna";
   char *p1=s,*p2=ch;
   
   while(*p1)
       p1++;
   while(*p2)
   {
       *p1=*p2;
       *p1++;
       *p2++;
   }
   
   printf("%s",s);
    return 0;
}

