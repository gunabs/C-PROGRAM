
#include <math.h>
#include <stdio.h>

int main()
{
    int N = 11011;

    int ans = 0;
    int i = 0;
    while (N != 0) {
        
        ans+=(N%10)*pow(2,i);
        N = N / 10;
        i++;
    }

    printf("%d", ans);

    return 0;
}
